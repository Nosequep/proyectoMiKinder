<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="valera.jesus.proyectomikinder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoMiKinder">
        <activity android:name=".RegistrarAgendaActivity"></activity>
        <activity android:name=".MostrarRecurso" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".MaestrosActivity" />
        <activity android:name=".ContactoActivity" />
        <activity android:name=".RecreoActivity" />
        <activity android:name=".RegistrarClase" />
        <activity android:name=".LlamadaActivity1" />
        <activity android:name=".recursosActivity" />
        <activity android:name=".AgendaActivity" />
        <activity android:name=".TrofeosActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".InicioActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>